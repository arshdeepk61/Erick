meta {
  name: Get User by Id
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/users/{{userId}}
  auth: none
}

tests {
  test("status is 200", function () {
    expect(res.status).to.equal(200);
  });
}
