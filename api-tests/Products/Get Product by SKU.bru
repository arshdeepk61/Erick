meta {
  name: Get Product by SKU
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/products/sku/WM-001
  auth: none
}

tests {
  test("status is 200", function () {
    expect(res.status).to.equal(200);
  });
}
