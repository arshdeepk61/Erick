meta {
  name: Get All Products
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/products?activeOnly=false
  auth: none
}

params:query {
  activeOnly: false
}

tests {
  test("status is 200", function () {
    expect(res.status).to.equal(200);
  });
}
