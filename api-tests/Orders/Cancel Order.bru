meta {
  name: Cancel Order
  type: http
  seq: 7
}

put {
  url: {{baseUrl}}/orders/{{orderId}}/cancel
  auth: none
}

tests {
  test("status is 200", function () {
    expect(res.status).to.equal(200);
  });
}
