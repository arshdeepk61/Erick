meta {
  name: Get Orders by Status
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/orders/status/PENDING
  body: none
  auth: none
}

tests {
  test("status is 200", function () {
    expect(res.status).to.equal(200);
  });
}

docs {
  Valid status values: PENDING, CONFIRMED, SHIPPED, DELIVERED, CANCELLED, REFUNDED
}
